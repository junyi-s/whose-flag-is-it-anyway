{
  "name": "whose-flag-is-it-anyway",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "dev": "pnpm --filter @whose-flag/shared build && concurrently \"pnpm --filter @whose-flag/web dev\" \"pnpm --filter @whose-flag/server dev\"",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.6.3"
  }
}
