[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter @whose-flag/shared build && pnpm --filter @whose-flag/server build"]

[start]
cmd = "node apps/server/dist/index.js"
